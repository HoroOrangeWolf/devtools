import { PageWrapper } from '@/components/pageWrapper.component.tsx';
import { CronContainer } from '@/container/cron/cron.container.tsx';

export const CronRoot = () => (
	<PageWrapper>
		<CronContainer />
	</PageWrapper>
);