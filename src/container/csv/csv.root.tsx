import { PageWrapper } from '@/components/pageWrapper.component.tsx';
import { CsvContainer } from '@/container/csv/csv.container.tsx';

export const CsvRoot = () => (
	<PageWrapper>
		<CsvContainer />
	</PageWrapper>
);