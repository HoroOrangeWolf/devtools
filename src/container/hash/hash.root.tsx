import { PageWrapper } from '@/components/pageWrapper.component.tsx';
import { HashContainer } from '@/container/hash/hash.container.tsx';

export const HashRoot = () => (
	<PageWrapper>
		<HashContainer />
	</PageWrapper>
);