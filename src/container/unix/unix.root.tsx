import { UnixContainer } from '@/container/unix/unix.container.tsx';
import { PageWrapper } from '@/components/pageWrapper.component.tsx';

export const UnixRoot = () => (
	<PageWrapper>
		<UnixContainer />
	</PageWrapper>
);