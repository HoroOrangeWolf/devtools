import { PageWrapper } from '@/components/pageWrapper.component.tsx';
import { Base64Container } from '@/container/base/base64.container.tsx';

export const Base64Root = () => (
	<PageWrapper>
		<Base64Container />
	</PageWrapper>
);